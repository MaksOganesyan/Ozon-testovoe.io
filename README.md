# Блок Progress — Тестовое задание Ozon

Прототип блока Progress для использования в мобильных web-приложениях. Блок отображает прогресс выполнения процессов и позволяет управлять его состоянием.

---

## 📋 Соответствие техническому заданию

### Назначение
> Разработать прототип блока Progress для использования в мобильных web-приложениях.  
> Основное предназначение блока — отображать процесс выполнения и его прогресс.

Блок реализован как круговой индикатор с дугой, отражающей значение прогресса (0–100%).

---

### Особенности из ТЗ

| Требование | Реализация |
|------------|------------|
| **API для управления состоянием** | Управление через DOM-события: `input`, `change`, `blur`. Элементы с `id="value"`, `id="animate"`, `id="hide"` доступны для программного управления. |
| **Переиспользуемость** | Модульная структура: отдельные HTML, CSS, JS без зависимостей. Блок легко встроить в любое приложение. |
| **Адаптация под ориентацию экрана** | Media queries для portrait (320×568) и landscape (568×320). |

---

### Состояния блока

#### 1. Normal (базовое)
- Значение **Value** (0–100) определяет размер дуги.
- Начало дуги — 12 часов (верхняя точка).
- Конец дуги при увеличении Value движется по часовой стрелке.
- При Value = 100 дуга образует полный круг.

#### 2. Animated
- Не зависит от Normal.
- Блок или его элементы вращаются по часовой стрелке с периодом 2 секунды.
- Включается/выключается переключателем **Animate**.

#### 3. Hidden
- Скрывает индикатор со страницы (`visibility: hidden`).
- Контролы остаются видимыми.
- Управляется переключателем **Hide**.

---

### Элементы управления (по макетам)

| Элемент | Тип | Описание |
|---------|-----|----------|
| **Value** | Текстовый ввод числа | Диапазон 0–100. Напрямую влияет на длину дуги. |
| **Animate** | Переключатель on/off | Включает/выключает анимацию вращения. |
| **Hide** | Переключатель on/off | Скрывает/показывает индикатор прогресса. |

---

### Технологии (согласно ТЗ)

| Технология | Использование |
|------------|---------------|
| **JS** | Vanilla JavaScript, без jQuery и других библиотек. |
| **CSS** | Обычный CSS, без пре/постпроцессоров. |
| **HTML** | Чистый HTML, без шаблонизаторов. |

---

### Результат и публикация

> Задача считается выполненной, если её можно открыть на телефоне по публичной ссылке на https://pages.github.com/

Проект готов к публикации на GitHub Pages. Инструкция ниже.

---

## 🚀 Быстрый старт

### Локальный запуск

1. Клонировать репозиторий или скопировать файлы.
2. Запустить локальный сервер в папке проекта, например:
   ```bash
   npx serve .
   ```
   или
   ```bash
   python -m http.server 8000
   ```
3. Открыть в браузере: `http://localhost:8000/main.html` или `http://localhost:3000/main.html`.

> Важно: для корректной работы лучше использовать локальный сервер, а не открывать HTML напрямую через `file://`.

---

## 📁 Структура проекта

```
ozon/
├── main.html    # Разметка страницы и блок Progress
├── main.js      # Логика индикатора и элементов управления
├── style.css    # Стили, анимации, адаптивность
└── README.md    # Документация
```

---

## 🔧 Описание файлов

### `main.html`
- HTML5 разметка с метатегами для мобильных устройств.
- Подключение шрифта Poppins.
- Блок Progress: индикатор (`.indicator`), контролы Value, Animate, Hide.
- Семантические атрибуты: `role="progressbar"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-label`.

### `main.js`
- Константы: `DEG_PER_PERCENT` (3.6), `MIN` (0), `MAX` (100).
- `setProgress(value)` — обновляет дугу и `aria-valuenow`.
- `syncInput()` — приводит Value к диапазону 0–100 при blur.
- Обработчики: `input`, `blur` для Value; `change` для Animate и Hide.
- Прогресс рисуется через `conic-gradient`.

### `style.css`
- CSS-переменные в `:root` для цветов и переходов.
- Стили для кругового индикатора (кольцо с белым центром).
- Кастомные переключатели (switch).
- `@keyframes rotate360` — анимация вращения.
- Media queries для portrait и landscape.

---

## 📱 Адаптивность

| Ориентация | Макет | Расположение |
|------------|-------|--------------|
| **Portrait** | 320×568 | Индикатор сверху (47%), контролы снизу (53%). |
| **Landscape** | 568×320 | Индикатор слева (47%), контролы справа (53%). |

---

## ♿ Доступность

- `role="progressbar"` — семантика индикатора прогресса.
- `aria-valuenow`, `aria-valuemin`, `aria-valuemax` — текущее значение и диапазон.
- `aria-label` — описание для скринридеров.
- `label[for]` — связь подписей с полями ввода.
- `:focus-visible` — видимая подсветка фокуса при навигации с клавиатуры.

---

## 🌐 Публикация на GitHub Pages

1. Создать репозиторий на GitHub.
2. Загрузить файлы: `main.html`, `main.js`, `style.css`.
3. Настройки репозитория → **Pages**.
4. Источник: **Deploy from a branch**.
5. Ветка: `main` (или `master`), папка: `/ (root)`.
6. Сохранить и дождаться сборки.

Страница будет доступна по адресу:  
`https://<username>.github.io/<repo>/main.html`

> Чтобы страница открывалась по корню, можно переименовать `main.html` в `index.html` или настроить redirect.

---

## 📐 Макеты

Проект реализован в соответствии с макетами:
- **Portrait 320×568** — вертикальное расположение.
- **Landscape 568×320** — горизонтальное расположение.

Цвета:
- Заполненная дуга: `#005bff`
- Пустая часть: `#eff3f6`
- Активный переключатель: `#005bff`

---

## 📄 Лицензия

Тестовое задание, образовательные цели.
